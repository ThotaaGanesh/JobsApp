import { SubscribeViewModel } from './subscribe-view-model';

describe('SubscribeViewModel', () => {
  it('should create an instance', () => {
    expect(new SubscribeViewModel()).toBeTruthy();
  });
});
