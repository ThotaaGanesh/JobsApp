import { NotificationViewModel } from './notification-view-model';

describe('NotificationViewModel', () => {
  it('should create an instance', () => {
    expect(new NotificationViewModel()).toBeTruthy();
  });
});
